import { NotifNumberPipe } from './notif-number.pipe';

describe('NotifNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NotifNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
